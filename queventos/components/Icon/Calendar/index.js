import Ionicons from '@expo/vector-icons/Ionicons';

export default ({size, color})=>{
    return (
      <Ionicons
        name="calendar-outline"
        size={size}
        color={color} />
    )
}