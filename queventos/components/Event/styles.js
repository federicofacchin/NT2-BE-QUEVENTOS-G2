import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        paddingTop: 8,
        paddingBottom: 8,
    },
    date: {
        flex: 1,
        color: "#374151"
    },
    body: {
        flex: 4
    }
  });

export default styles