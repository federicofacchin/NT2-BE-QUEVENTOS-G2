import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        padding: 16
    },
    action: {
        color: "#0ea5e9",
        marginRight: 4
    }
  });

export default styles